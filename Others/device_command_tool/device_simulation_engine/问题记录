1、引擎设计实现分层是否合理?
    1)功能划分模块
        device_simulation_engine/
        ├── app/
        │   ├── __init__.py               # Flask 应用初始化
        │   ├── api/                      # API 接口层
        │   │   ├── channel_camera.py

        │   ├── services/                 # 业务逻辑层,负责处理业务逻辑，调用model层组装协议包，调用connection发送，处理服务器下发数据等
        │   │   ├── channel_camera_service.py

        │   ├── models/                   # 数据模型层
        │   │   ├── channel_camera_model.py     # 负责各种类型的协议包组装

        │   ├── connection/               # 连接层
        │   │   └── tcp_connection.py     # TCP 连接层，只处理：1.连接相关的功能和逻辑2.负责发送和接收原始数据，不对数据做任何逻辑处理

        │   ├── utils/                    # 工具模块
        │   │   ├── __init__.py
        │   │   ├── logger.py             # 日志工具
        │   │   └── config.py             # 配置加载工具

        ├── config.yml
        ├── requirements.txt
        ├── run.py

    2）api层的数据校验纯手写，感觉很冗余

    3)每一层的职责范围
        api层是否应该负责数据转化？比如接收参数类型转化，还是说用这个参数的地方自己去转


2、关于抽象类：
    是否需要抽象出基类？如果抽象，每一层的子类又各自需要自己打不同的日志，每个功能函数其实还是要写，怎么解决？
        自己实现一个日志装饰器，子类中声明父类方法的时候用每个类自己的日志装饰器？

3、关于日志：
    1）哪些地方是需要打的
        接口的出入口？但是这种设计不是接口互相调用，又不存在返回值
    2）层级怎么规划
        哪些层应该要打，或者说哪些层该打什么等级的？
    3）颗粒度怎么划分？是否应该打堆栈？
        比如，需要校验的时候，每一种校验都要单独分日志？或者多重条件判断直接一次性打一个
    4）每个地方如果都要用try except包含，写起来非常冗余，有啥其他办法没

